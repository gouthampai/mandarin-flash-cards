// No-op module stub used to neutralize Expo's winter runtime in Jest.
// The winter runtime installs lazy getters (TextDecoder, URL, structuredClone, etc.)
// that call require() outside jest-runtime's active module execution context,
// causing "import outside scope" errors. We don't need these web-compat polyfills
// for React Native component tests.
